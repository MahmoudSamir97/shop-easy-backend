// const express = require('express');
// const validate = require('../Middlewares/Valiadate');
// const productSchema = require('../joi Validations/productSchema');
// const { createProduct, updateProduct, deleteProduct } = require('../Controllers/productController');
// const { updateUser, getDashboard, changeRole, deactivateAccount } = require('../Controllers/adminController');
// const upload = require('../Middlewares/multer');
// const adminRoutes = express.Router();
// adminRoutes.route('/product').post(upload.single('image'), validate(productSchema), createProduct);
// adminRoutes.route('/product/:productName').patch(upload.single('image'), updateProduct).delete(deleteProduct);
// adminRoutes.route('/user/role').patch(changeRole);
// adminRoutes.route('/user/deactivate').patch(deactivateAccount);
// adminRoutes.route('/user/update/:id').patch(updateUser);

// module.exports = adminRoutes;
